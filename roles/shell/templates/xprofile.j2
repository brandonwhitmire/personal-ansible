#!/bin/sh

# Restore display configuration early in X session
# This runs before window managers and other GUI applications
# The restore-display.sh script contains xrandr commands generated by display_auto

if [ -f "$HOME/.config/display/restore-display.sh" ]; then
    "$HOME/.config/display/restore-display.sh" 2>/dev/null
fi

