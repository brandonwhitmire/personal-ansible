# Reference: https://keepassxc.org/docs/KeePassXC_GettingStarted.html#_setup_browser_integration
# This playbook will ensure the whole KeepassXC setup is installed
- name: Install KeepassXC password manager and required packages
  become: true
  # https://docs.ansible.com/ansible/latest/collections/community/general/pacman_module.html
  community.general.pacman:
    name: 
      - keepassxc
      - cmake
      - make
    state: present
- name: Install Librewolf package
  # https://github.com/mnussbaum/ansible-yay
  yay:
    name: librewolf-bin
    state: present
- name: Copy browser config to allow keepassxc-browser extention to communicate
  ansible.builtin.copy:
    src: '{{ lookup("file", "../files/browser/native-messaging-hosts") }}'
    dest: ~/.librewolf/
- name: Pull keepassxc-proxy repo to build binary
  # https://github.com/varjolintu/keepassxc-proxy
  ansible.builtin.git:
    repo: 'https://github.com/varjolintu/keepassxc-proxy.git'
    dest: /tmp/
    clone: true
- name: Generate keepassxc-proxy Makefiles
  ansible.builtin.shell:
    cmd: cmake -DCMAKE_BUILD_TYPE=Release .
    chdir: /tmp/keepassxc-proxy/qt_version/
- name: Build keepassxc-proxy binary
  ansible.builtin.shell:
    cmd: make
    chdir: /tmp/keepassxc-proxy/qt_version/
- name: Copy keepassxc-proxy binary into /usr/bin/
  become: true
  ansible.builtin.copy:
    src: /tmp/keepassxc-proxy/qt_version/keepassxc-proxy
    dest: /usr/bin/keepassxc-proxy
    owner: root
    group: root
    mode: '0755'
- name: Copy keepassxc-proxy systemd unitfile file to /etc/
  become: true
  ansible.builtin.copy:
    src: '{{ lookup("file", "../files/systemd/keepassxc-proxy.service") }}'
    dest: /etc/systemd/system/
- name: Enable keepassxc-proxy service
  become: true
  ansible.builtin.service:
    name: keepassxc-proxy
    enabled: true
    state: started
