---
- name: Prepare and configure all hosts
  hosts: all
#  gather_facts: false
#  pre_tasks:
#    - name: 'Gather facts (much more quietly)'
#      ansible.builtin.setup:
#      # no_log: true
#    - name: Display target OS
#      ansible.builtin.debug:
#        msg: 'TARGET OS: {{ ansible_facts["distribution"] }}'
  roles:
    - baseline
    - arch_recommendations
    - i3
    - shell
    - theme
    - conky
    - nitrogen
    - keepassxc
    - powertop
    - locale
    - docker
    - grub
    - calibre
    - virtualization
    - vagrant
    # NOTE: steam must be at the end since it upgrades the system,
    # which breaks some other things until a reboot is completed
    - steam
